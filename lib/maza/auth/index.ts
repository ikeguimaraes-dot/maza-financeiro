// @maza/auth — Authentication utilities for MAZA
// Import specific exports via subpaths:
//   @maza/auth/context  — useAuth(), useUnit() (Client Component)
//   @maza/auth/server   — requireUser(), getCurrentUser(), requireRole(), isFounder()
//   @maza/auth/unit     — getCurrentUnit()
